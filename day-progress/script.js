const time=document.getElementById("time"),date=document.getElementById("date"),bar=document.getElementById("bar"),percent=document.getElementById("percent"),remaining=document.getElementById("remaining");
function tick(){const d=new Date(),start=new Date(d.getFullYear(),d.getMonth(),d.getDate()),end=new Date(d.getFullYear(),d.getMonth(),d.getDate()+1);const pct=((d-start)/(end-start))*100;time.textContent=d.toLocaleTimeString("en-GB",{hour:"2-digit",minute:"2-digit"});date.textContent=d.toLocaleDateString("en-US",{weekday:"long",month:"long",day:"numeric"});bar.style.width=pct.toFixed(2)+"%";percent.textContent=pct.toFixed(1)+"%";const mins=Math.max(0,Math.round((end-d)/60000));remaining.textContent=Math.floor(mins/60)+"h "+(mins%60)+"m left";}
tick();setInterval(tick,1000);